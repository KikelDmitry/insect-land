include _meta.pug
doctype html
html(lang="ru")
    head
        title insect-page landing page
        link(rel="stylesheet" href=dist + "css/main.css")
    body
        h1 happy quarantine, bitches!
